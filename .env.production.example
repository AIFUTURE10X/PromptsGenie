# =============================================================================
# GOOGLE API CONFIGURATION FOR PRODUCTION (NETLIFY)
# =============================================================================
# See GOOGLE_API_SETUP.md for detailed setup instructions
#
# For Netlify deployment, set these in: Site settings > Environment variables
# =============================================================================

# Google API Key for Gemini API (text and image generation)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Google Cloud Project ID (required for Vertex AI image generation)
# Find this in Google Cloud Console
GOOGLE_PROJECT_ID=your_google_project_id_here

# Google Application Credentials (Service Account JSON as a single-line string)
# IMPORTANT: This must be the entire JSON file minified to a single line
# Steps:
# 1. Go to Google Cloud Console > IAM & Admin > Service Accounts
# 2. Create a service account with "Vertex AI User" role
# 3. Create and download JSON key
# 4. Minify the JSON: cat credentials.json | jq -c
# 5. Paste the entire minified JSON as the value here
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...","client_id":"..."}

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# These are embedded in the client build (prefixed with VITE_)
# They are accessible in the browser, so don't put sensitive data here

# Gemini API Key for frontend (can be same as GOOGLE_API_KEY or different)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Gemini Models to use
VITE_GEMINI_MODEL_TEXT=gemini-1.5-flash
VITE_GEMINI_MODEL_IMAGES=gemini-2.5-flash
VITE_GEMINI_MODEL_IMAGE=gemini-2.5-flash

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# API Base URL (for Netlify Functions, defaults to /.netlify/functions)
# API_BASE_URL=/.netlify/functions

# Supabase Configuration (optional)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your_supabase_anon_key

# Image Analysis Settings
# IMAGE_ANALYSIS_ENABLED=true
# IMAGE_ANALYSIS_MAX_SIZE_MB=4
# IMAGE_ANALYSIS_TIMEOUT_MS=30000
